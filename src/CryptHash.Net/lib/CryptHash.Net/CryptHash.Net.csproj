<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>netstandard2.0;netstandard2.1;netcoreapp3.0</TargetFrameworks>
    <Authors>Alessandro Cagliostro Goncalves Neves</Authors>
    <Description>CryptHash.Net is a .NET Standard 2.0 Class Library (can be used in projects with any .NET implementation like  .NET Framework, .NET Core, Mono, Xamarin, etc.) for text and files symmetric (AES_128_CBC / AES_192_CBC / AES_256_CBC) authenticated (HMAC_SHA_256 / HMAC_SHA_384 / HMAC_SHA_512) encryption/decryption with salt, and text/files hashing (MD5, SHA1, SHA256, SHA384, SHA512, and BCrypt).</Description>
    <Copyright>Alessandro Cagliostro Goncalves Neves, 2019</Copyright>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageIconUrl>https://raw.githubusercontent.com/alecgn/crypthash-net/master/src/CryptHash.Net/lib/CryptHash.Net/Icon/crypthash-icon.png</PackageIconUrl>
    <RepositoryUrl>https://github.com/alecgn/crypthash-net</RepositoryUrl>
    <PackageTags>encryption decryption cryptography aes aes128 aes256 aes192 aes128cbc aes256cbc aes192cbc aescbc aescbc128 aescbc256 aescbc192 cbc hash md5 sha1 sha256 sha384 sha512 bcrypt c# c-sharp security password authentication authenticated</PackageTags>
    <AssemblyVersion>2.1.0</AssemblyVersion>
    <FileVersion>2.1.0</FileVersion>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <PackageProjectUrl>https://github.com/alecgn/crypthash-net</PackageProjectUrl>
    <Version>2.1.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BCrypt.Net-Next" Version="3.1.3" />
  </ItemGroup>

</Project>
