<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>netcoreapp3.0</TargetFramework>
    <Version>3.2.0</Version>
    <AssemblyVersion>3.2.0</AssemblyVersion>
    <FileVersion>3.2.0</FileVersion>
    <Authors>Alessandro Cagliostro Goncalves Neves</Authors>
    <Description>crypthash is a .NET Core 3.0 multiplatform console utility (designed to run in Windows, Linux and Mac) for text and files symmetric (AES_128_CBC / AES_192_CBC / AES_256_CBC / AES_256_GCM) authenticated encryption/decryption, and text/files hashing (MD5, SHA1, SHA256, SHA384, SHA512, PBKDF2, BCrypt and Argon2id).</Description>
    <Copyright>Alessandro Cagliostro Goncalves Neves, 2019</Copyright>
    <PackageProjectUrl>https://github.com/alecgn/crypthash-net</PackageProjectUrl>
    <PackageIconUrl>https://raw.githubusercontent.com/alecgn/crypthash-net/master/src/CryptHash.Net/lib/CryptHash.Net/Icon/crypthash-icon.png</PackageIconUrl>
    <RepositoryUrl>https://github.com/alecgn/crypthash-net</RepositoryUrl>
    <PackageTags>encryption decryption cryptography aes aes128 aes256 aes192 aes128cbc aes256cbc aes192cbc aescbc aescbc128 aescbc256 aescbc192 cbc hash md5 sha1 sha256 sha384 sha512 pbkdf2 bcrypt argon2 argon2id c# c-sharp security password authentication authenticated</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="CommandLineParser" Version="2.6.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\lib\CryptHash.Net\CryptHash.Net.csproj" />
  </ItemGroup>

</Project>
